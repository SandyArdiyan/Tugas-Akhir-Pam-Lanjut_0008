name: tugasakhirpam
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

  # --- Tambahan Package Tugas Akhir PAM ---
  # State Management
  flutter_bloc: ^8.1.3
  equatable: ^2.0.5          
  
  # Network & API
  dio: ^5.4.3                
  
  # Routing
  go_router: ^13.2.0         
  
  # Security
  flutter_secure_storage: ^9.0.0 
  rxdart: ^0.28.0
  url_launcher: ^6.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  
  # Splash Screen
  flutter_native_splash: ^2.3.10

flutter:
  uses-material-design: true